- Go to lambda and create a function 
- Use a Blueprint to create a function
- Then  choose blueprint name as hello world function with python
- Give function name as HelloWorld
- In execution role
    - Your lambda function is going to be having execution role. So we choose to create a new role with basic lambda permissions
- Create function

- We can now test our function 
- In the cloudwatch , we can get logs of our function where we can debug our function
- Under the configuration tab , we can set Memory , ephemeral storage, timeout (how long you want it to run)