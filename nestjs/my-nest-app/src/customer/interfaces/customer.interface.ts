// here we define type that inside my application , i will be handling the customer data here, what type of data of that customer do we want to keep i.e, which contract or which template will that customer data follow

export interface Customer{
    id: number
    name: string
    age: number
}