// enums in typescript is used when we have to define our constant options that only these values will be included 

// this is our enum in which we have only 2 options User and Admin. User having value user and Admin having value admin
export enum Role { 
    User = 'user',
    Admin = 'admin'
}